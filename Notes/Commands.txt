git config --global user.name "kdr-code"
git config --global user.email "kdr.anandini@gmail.com"

git config --list

git init

git status

git add .

git commit -m "commit 1"

git remote add origin https://github.com/kdr-code/FSAD.git

git branch

git push -u origin master

git checkout master
(to access master branch)

git merge frontend -m "merging frontend to master"

git merge backend -m "merging backend to master"